
/**
 * @author (Zvi Barak)
 * @version (a version number or a date)
 * 
 * Folder Library includes:
 *  Point,
 *  Print
 *  
 * Folder תרגילים נוספים includes:
 *      -- using Recursion --
 *  Recursion.permutation,
 *  Recursion.powerSet,
 *  Recursion.findInterleavings() of 2 Strings,
 *  Recursion.factorial
 *   -- using loops (while or for) -- 
 *  EditDistance.editDistance Approximated String Matching,
 *  ConwayGameOfLife - leave or die based on neighbours,
 *  LastMatch - pick matches from the table usinl none,
 *  Disco - turn on/off bulbs to see if set 1 of bulbs can become set 2,
 *  howMany - number of times a subsring appear in a string,
 *  longestArrow - find longest subset of integers in acsending order
 *  
 *  Folder ממן 14 includes: -- using Tree and loops (while or for)--
 *      MatrixList.getData_i_j,
 *      MatrixList.setData_i_j,
 *      MatrixList.findMax - largest integer,
 *      MatrixList.howManyX - how many times X in matrix where each row and 
 *          columns are in acsending order, in an efficient way
 *      
 *  Folder ממן 13 includes:  -- using Recursion --
 *      Ex13.extreme - find largest number in all possible paths from
 *          top left to bottom right, moving up/down/left/right
 *      Ex13.first
 *      Ex13.longestNearlyPalindrome
 *        -- most efficient method --
 *      Ex13.specialArr - arrange array with less and more neighbours
 *  
 */
